<li class="image" image_id="images_{{ image.id }}">
  {{ image | uploaded_image('blog_admin_preview') }}
  <a class="btn btn-danger btn-small" href="{{ path('admin_site_base_blogimage_delete', { id: image.id }) }}">{{ 'delete?' | trans }}</a>
</li>